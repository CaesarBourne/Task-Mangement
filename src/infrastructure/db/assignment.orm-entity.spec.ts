import { AssignmentOrmEntity } from './assignment.orm-entity';

describe('AssignmentOrmEntity', () => {
  it('should be defined', () => {
    expect(new AssignmentOrmEntity()).toBeDefined();
  });
});
